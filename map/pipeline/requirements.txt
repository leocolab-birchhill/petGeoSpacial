# Map pipeline deps — align versions with ../../requirements.txt where shared
pandas>=2.0
polars>=0.20
pyarrow>=14
h3>=4.0
shapely>=2.0
scikit-learn>=1.3   # BallTree haversine (same pattern as scripts/prepare_routing_inputs.py)
openpyxl>=3.1       # Excel competitor sheet
numpy>=1.26
# Windows PMTiles builder (no tippecanoe/Docker/WSL required)
pmtiles>=3.0
mapbox-vector-tile>=2.0
mercantile>=1.2
protobuf>=4.0
